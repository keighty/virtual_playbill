import React from 'react';
import ReactDOM from 'react-dom'

class SearchBar extends React.Component {
  render() {
    return <input type="text" placeholder='Search shows, actors, company, etc' />
  }
}

export default SearchBar
